name: P2P
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - test
      - test_2

jobs:
  p2p:
    uses: coreeng/reusable-p2p/.github/workflows/idp-p2p.yaml@test_dev_dev_deploy
    with:
      project-id: ${{ vars.PROJECT_ID }}
      project-number: ${{ vars.PROJECT_NUMBER }}
      tenant-name: ${{ vars.TENANT_NAME }}
