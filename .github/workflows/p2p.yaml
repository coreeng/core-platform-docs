name: P2P
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - test_tags
      
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
    - name: Printenv
      run: |
        printenv 
